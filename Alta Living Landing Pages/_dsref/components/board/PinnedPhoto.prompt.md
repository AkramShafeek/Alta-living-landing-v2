Photo-as-object for the hero collage — the only place Alta uses soft photographic shadows.

```jsx
<PinnedPhoto src="assets/homes/1.jpg" seed={37} caption="Indiranagar" />
```

Absolutely position several inside a relative container to build a collage; hover straightens the tilt to 40% and lifts the shadow.
