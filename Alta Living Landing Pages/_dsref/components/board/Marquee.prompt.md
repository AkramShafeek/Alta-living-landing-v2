Fact ticker that sits directly under or above the hero. Copy is lowercase, factual, and specific.

```jsx
<Marquee items={["14 homes live across bangalore","new listing — indiranagar 2bhk","average move-in time — 3 days"]} />
```

Needs the `alta-marquee` keyframes from `tokens/base.css`.
