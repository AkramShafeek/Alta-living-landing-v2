The only background texture in the system. Put it as the first child of a `position:relative` hero.

```jsx
<div style={{position:'relative'}}><PatternField /> …</div>
```

Keep opacity ≤ 0.12; the doodles must never compete with type.
