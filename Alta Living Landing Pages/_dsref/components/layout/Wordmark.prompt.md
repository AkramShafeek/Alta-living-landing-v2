Always use the supplied mark file — never redraw it.

```jsx
<Wordmark variant="mark" size={48} />
<Wordmark variant="condensed" size={96} />
```

The boxed mark (white box, 1px ink border, 12px radius, 3px hard shadow) is the header treatment.
